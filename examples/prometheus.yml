# Example Prometheus scrape config for Neural Blitz NG monitor mode
global:
  scrape_interval: 30s

scrape_configs:
  - job_name: neural-blitz
    metrics_path: /metrics/prometheus
    static_configs:
      - targets:
          - 127.0.0.1:8888
